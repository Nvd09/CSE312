version: '3'
services:
  app:
    container_name: social-media-app
    restart: always
    build: .
    ports:
      - '80:8000'
    links:
      - mongo
  mongo:
    container_name: mongo_database
    image: mongo
    ports:
      - '27017:27017'